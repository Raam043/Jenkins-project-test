---
- hosts: rnb
  become: True
  tasks:
              
    - name: Run docker container
      docker_container:
        name: WebServer
        image: raam043/nginx
        state: started
        exposed_ports:
        - "80"
        ports:
        - "80:80"
